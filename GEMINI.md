# Gemini repository adapter

Follow `AGENTS.md` as the repository-wide operational authority.

When working under `backend/tests/**`, also follow `backend/tests/AGENTS.md`.

Durable architecture, testing, benchmark, security, ingestion, and legal-evidence contracts live under `docs/`; do not treat this file as an independent architecture manual.
