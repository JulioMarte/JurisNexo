# Gemini migration adapter

This file is an adapter, not an independent migration manual.

For `backend/migrations/**`, follow repository `AGENTS.md`, then `backend/migrations/AGENTS.md`, plus the current corpus/data and testing contracts under `docs/`. Treat migration history and PostgreSQL constraints as correctness-sensitive evidence.
