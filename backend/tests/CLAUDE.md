# Claude test adapter

This file is an adapter, not an independent testing manual.

For `backend/tests/**`, follow repository `AGENTS.md`, then `backend/tests/AGENTS.md`, and the canonical testing contracts under `docs/testing/`. Do not weaken a proof to satisfy the current implementation; disposition the protected guarantee explicitly.
