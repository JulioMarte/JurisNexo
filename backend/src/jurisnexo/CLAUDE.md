# Claude adapter

This file is an adapter, not an independent architecture manual.

For `backend/src/jurisnexo/**`, follow repository `AGENTS.md`, then `backend/src/jurisnexo/AGENTS.md`, and the current contracts under `docs/`. The nearer AGENTS rules win only by adding stricter path-specific guidance; they must not contradict repository policy.
