name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  backend-database:
    name: Backend database contract
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - name: Build backend test image
        run: docker compose --profile test build test
      - name: Start database and apply migrations
        run: docker compose up -d db && docker compose run --rm migrate
      - name: Run backend schema contract tests
        run: >-
          docker compose --profile test run --rm
          -v "$PWD:/repo:ro"
          -e JURISNEXO_REPO_ROOT=/repo
          -e GITHUB_BASE_REF
          -e GITHUB_HEAD_REF
          test pytest -q
      - name: Show service state on failure
        if: failure()
        run: docker compose ps && docker compose logs --no-color db migrate
      - name: Tear down
        if: always()
        run: docker compose --profile test down -v --remove-orphans

  backend-quality:
    name: Backend quality
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Build backend test image
        run: docker compose --profile test build test
      - name: Ruff
        run: docker compose --profile test run --rm --no-deps test ruff check src tests migrations
      - name: Architecture fitness functions
        run: >-
          docker compose --profile test run --rm --no-deps
          -v "$PWD:/repo:ro"
          -e JURISNEXO_REPO_ROOT=/repo
          -e GITHUB_BASE_REF
          -e GITHUB_HEAD_REF
          test pytest tests/architecture
      - name: Ruff ingestion benchmark runners
        run: >-
          docker compose --profile test run --rm --no-deps
          -v "$PWD/benchmark:/benchmark:ro"
          test ruff check /benchmark/ingestion
      - name: Historical benchmark scorer smoke test
        run: |
          docker compose --profile test run --rm --no-deps \
            -v "$PWD/benchmark:/benchmark:ro" \
            test sh -ec '
              python /benchmark/ingestion/evaluate_historical_bulletin_agent.py \
                --result /benchmark/ingestion/fixtures/historical_agent_score.pass.json \
                --gold /benchmark/ingestion/gold/historical_bulletin_831_index.v1.json \
                --sha256 /benchmark/ingestion/fixtures/historical_agent_score.sha256.txt \
                --output /tmp/historical-agent-score.json
              python -c "import json; p=json.load(open(\"/tmp/historical-agent-score.json\")); assert p[\"status\"] == \"PASS\"; assert p[\"semantic_status\"] == \"PASS\"; assert p[\"metrics\"][\"verified_gold_reference_count\"] == 3; assert p[\"semantic_metrics\"][\"confirmed_gold_reference_count\"] == 3"
              python /benchmark/ingestion/evaluate_historical_bulletin_agent.py \
                --result /benchmark/ingestion/fixtures/historical_agent_score.unsupported_semantic.json \
                --gold /benchmark/ingestion/gold/historical_bulletin_831_index.v1.json \
                --sha256 /benchmark/ingestion/fixtures/historical_agent_score.sha256.txt \
                --output /tmp/historical-agent-score-unsupported.json
              python -c "import json; p=json.load(open(\"/tmp/historical-agent-score-unsupported.json\")); assert p[\"status\"] == \"PASS\"; assert p[\"semantic_status\"] == \"FAIL\"; assert p[\"semantic_metrics\"][\"confirmed_gold_reference_count\"] == 2; assert p[\"semantic_metrics\"][\"investigations\"][2][\"trace_supported\"] is False"
              python /benchmark/ingestion/evaluate_historical_bulletin_agent.py \
                --result /benchmark/ingestion/fixtures/historical_agent_score.delegated.json \
                --gold /benchmark/ingestion/gold/historical_bulletin_831_index.v1.json \
                --sha256 /benchmark/ingestion/fixtures/historical_agent_score.sha256.txt \
                --output /tmp/historical-agent-score-delegated.json
              python -c "import json; p=json.load(open(\"/tmp/historical-agent-score-delegated.json\")); assert p[\"status\"] == \"PASS\"; assert p[\"semantic_status\"] == \"PASS\"; assert p[\"semantic_metrics\"][\"confirmed_gold_reference_count\"] == 3; assert p[\"metrics\"][\"delegated_printed_page_attempts\"] == [192,193,194]; assert p[\"metrics\"][\"verified_printed_pages\"] == [183,189,193]"
            '
      - name: Targeted boundary discrepancy scorer smoke test
        run: |
          docker compose --profile test run --rm --no-deps \
            -v "$PWD/benchmark:/benchmark:ro" \
            test sh -ec '
              python /benchmark/ingestion/evaluate_targeted_boundary_discrepancy.py \
                --result /benchmark/ingestion/discrepancy/result_353.pass.json \
                --target /benchmark/ingestion/discrepancy/target_353.v1.json \
                --gold /benchmark/ingestion/discrepancy/gold_353.v1.json \
                --output /tmp/discrepancy-pass.json
              python -c "import json; p=json.load(open(\"/tmp/discrepancy-pass.json\")); assert p[\"status\"] == \"PASS\"; assert p[\"metrics\"][\"claimed_destination_read\"] is True; assert p[\"metrics\"][\"mismatch_recognized\"] is True; assert p[\"metrics\"][\"resolution_matches_gold\"] is True; assert p[\"metrics\"][\"trace_supported\"] is True"
              python /benchmark/ingestion/evaluate_targeted_boundary_discrepancy.py \
                --result /benchmark/ingestion/discrepancy/result_353.fail_direct_acceptance.json \
                --target /benchmark/ingestion/discrepancy/target_353.v1.json \
                --gold /benchmark/ingestion/discrepancy/gold_353.v1.json \
                --output /tmp/discrepancy-fail.json
              python -c "import json; p=json.load(open(\"/tmp/discrepancy-fail.json\")); assert p[\"status\"] == \"FAIL\"; assert p[\"metrics\"][\"claimed_destination_read\"] is True; assert p[\"metrics\"][\"neighbors_investigated\"] is False; assert p[\"metrics\"][\"mismatch_recognized\"] is False; assert p[\"metrics\"][\"resolution_matches_gold\"] is False"
            '
      - name: Structure Auditor scorer smoke test
        run: |
          docker compose --profile test run --rm --no-deps \
            -v "$PWD/benchmark:/benchmark:ro" \
            test sh -ec '
              python /benchmark/ingestion/evaluate_structure_auditor.py \
                --result /benchmark/ingestion/fixtures/structure_auditor.synthetic.pass.json \
                --gold /benchmark/ingestion/gold/structure_auditor.synthetic.v1.json \
                --output /tmp/structure-auditor-pass.json
              python -c "import json; p=json.load(open(\"/tmp/structure-auditor-pass.json\")); assert p[\"status\"] == \"PASS\"; assert p[\"metrics\"][\"error_detection_recall\"] == 1.0; assert p[\"metrics\"][\"false_rejection_rate\"] == 0.0; assert p[\"metrics\"][\"total_auditor_tokens\"] > 0; assert p[\"metrics\"][\"total_auditor_latency_ms\"] > 0"
              python /benchmark/ingestion/evaluate_structure_auditor.py \
                --result /benchmark/ingestion/fixtures/structure_auditor.synthetic.fail_false_rejection.json \
                --gold /benchmark/ingestion/gold/structure_auditor.synthetic.v1.json \
                --output /tmp/structure-auditor-fail.json
              python -c "import json; p=json.load(open(\"/tmp/structure-auditor-fail.json\")); assert p[\"status\"] == \"FAIL\"; assert p[\"metrics\"][\"error_detection_recall\"] == 1.0; assert p[\"metrics\"][\"false_rejection_rate\"] == 0.5"
            '
      - name: Pyright
        run: docker compose --profile test run --rm --no-deps test pyright

  docker-runtime:
    name: Docker runtime smoke test
    needs: backend-database
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Start API stack
        run: docker compose up -d --build db migrate api
      - name: Wait for API health
        run: |
          for i in {1..30}; do
            if curl --fail --silent http://127.0.0.1:8000/health/live >/dev/null; then
              exit 0
            fi
            sleep 2
          done
          docker compose ps
          docker compose logs --no-color api migrate db
          exit 1
      - name: Tear down
        if: always()
        run: docker compose down -v --remove-orphans

  aggregate:
    name: CI aggregate
    if: always()
    needs: [backend-database, backend-quality, docker-runtime]
    runs-on: ubuntu-24.04
    steps:
      - name: Require all jobs to pass
        env:
          NEEDS: ${{ toJSON(needs) }}
        run: |
          python - <<'PY'
          import json, os, sys
          needs = json.loads(os.environ['NEEDS'])
          failed = {name: data['result'] for name, data in needs.items() if data['result'] != 'success'}
          if failed:
              print(f"Required CI jobs did not pass: {failed}")
              sys.exit(1)
          print('All required CI jobs passed.')
          PY